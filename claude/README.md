# Claude

Link `./statusline.sh` to `~/.claude/statusline.sh` and set "statusLine" attribute in `~/.claude/settings.json`.

```json
"statusLine": {
  "type": "command",
  "command": "~/.claude/statusline.sh"
}
```
